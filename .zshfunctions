function debitly()
{
       dotnet /home/alumni/Documents/Cloud/SourceCodes/Debitly/bin/Debug/netcoreapp2.0/Debitly.dll -installed-debitly $@
}

function duke()
{
       dotnet /home/alumni/Documents/Cloud/SourceCodes/LundukeApp/bin/Debug/netcoreapp2.0/LundukeApp.dll -installed-duke $@
}
yaourtClean() {
	echo 'shit'
    yaourt "$@"
    sudo rm -rf /var/cache/pacman
}
calc () {
    bc -l <<< "$@"
    }


gitout() {
    git add .
    if [ "$1" != "" ]
    then
        git commit -m "$1"
    else
        git commit -m update
    fi
    git push
}

proxy()
{
	if [ "$1" = "on" ]
	then
		 sudo systemctl start tor
		 export http_proxy=socks5://127.0.0.1:9050/
		 export https_proxy=$http_proxy
		 export ftp_proxy=$http_proxy
		 export rsync_proxy=$http_proxy
		 export no_proxy="localhost,127.0.0.1,localaddress,.localdomain.com"
		 tor-browser
	else
		 sudo systemctl stop tor
		 unset http_proxy
		 unset https_proxy
		 unset ftp_proxy
		 unset rsync_proxy
		 unset no_proxy
	fi
}